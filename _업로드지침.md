# 22 냉온배관 v5.0.1 — GitHub 업로드 지침

작성 2026-09-01 · 저장소 `manminkim-eng/CHWS-STSTEM` · 브랜치 `main`

---

## 1. 업로드 대상 2건

| 파일 | 현 배포본 | 업로드본 | 구분 |
|---|---|---|---|
| `index.html` | 193,208 B | **193,879 B** | 변경 |
| `sw.js` | 4,324 B | **4,871 B** | 변경 |

```
index.html  4b8eb5b101be3b557bb732a9326dbece203d3015
sw.js       (PREFIX 처방 반영본 · pipe-v5.0.8)
```

`manifest.json` · `icons/` · `assets/` 는 손대지 않는다.

---

## 2. 무엇을 고쳤나 — 2건

### ① JPG 쪽 나눔을 인쇄와 같은 「블록 원자」 방식으로

MIN 지적 — 24 설비펌프에서 **PDF 6쪽 · JPG 5매**로 쪽 나눔이 어긋났다.
같은 결함이 22번에도 있었다.

| | 종전 | 변경 |
|---|---|---|
| 방식 | 유효높이까지 채운 뒤 안전절단선에서 자름 | **블록이 남은 공간에 다 안 들어가면 통째로 다음 장** |
| 원자 단위 | 표만 보호 | `#a4Page` 직계 자식 · **소제목(.a4-sub)은 뒤따르는 표와 한 덩어리** |
| 예외 | — | 블록 하나가 한 장보다 크면 그때만 내부 절단 |

폐기 : `mmPickCutPreferSection` · `mmSectionTops` · `mmTableRanges`
신설 : `mmBlocks()` · `mmSegments()` 재작성

소제목 묶음이 중요하다. 안 묶으면 `2. 관경 산정 결과` 같은 제목만 앞 쪽 끝에 남고
표가 다음 장으로 넘어간다. 인쇄의 `.a4-sub + table.a4-tbl{break-before:avoid}` 와 같은 동작이다.

### ② ⛔ 캐시 삭제 범위를 자기 접두어로 한정

```js
/* 종전 — origin 전체를 훑어 남의 캐시까지 지운다 */
keys.filter(k => k !== CACHE)

/* 변경 */
const PREFIX = 'pipe-';
keys.filter(k => k !== CACHE && k.indexOf(PREFIX) === 0)
```

`caches.keys()` 는 **origin 전체**를 반환한다.
`manminkim-eng.github.io` 한 origin 에 39종이 배포돼 있어,
**어느 도구를 열든 나머지 38종의 캐시를 전부 지우고 있었다.**
서로 지우고 다시 받는 일이 반복돼 오프라인 동작이 사실상 무력화된 상태였다.

> 이 결함은 25 급탕설비 오업로드 사고를 추적하다 발견했다.
> 22 · 24 · 25 세 종 모두 같은 처방을 넣었다.

캐시명 `pipe-v5.0.7` → **`pipe-v5.0.8`**

---

## 3. 업로드 후 검증

```js
javascript:(async()=>{const R='https://manminkim-eng.github.io/CHWS-STSTEM/';
const g=async p=>{const x=await fetch(R+p+'?t='+Date.now(),{cache:'no-store'});return x.ok?await x.text():null;};
const h=await g('index.html'),s=await g('sw.js');
console.table({
 '블록 쪽나눔':/function mmBlocks/.test(h||''),
 '소제목 묶음':/classList\.contains\('a4-sub'\)/.test(h||''),
 '구 절단로직 제거':!/mmPickCutPreferSection/.test(h||''),
 'SW 캐시명':(s||'').match(/CACHE\s*=\s*['"]([^'"]+)/)?.[1],
 'SW 접두어 한정':/indexOf\(PREFIX\) === 0/.test(s||''),
 'SW navigate분기':/destination === 'document'/.test(s||'')
});})()
```

**기대값** — `SW 캐시명` 이 `"pipe-v5.0.8"`, 나머지 전부 `true`

---

## 4. 확인할 것 — 배포 후

1. A4 미리보기에서 **PDF 쪽수와 JPG 매수가 같은지**
2. 각 쪽 시작이 서로 같은 소제목에서 시작하는지
3. 소제목만 앞 쪽 끝에 남는 일이 없는지

---

## 5. 손대지 않은 것

- 계산 로직 (관경 산정식 · 재질 상수 · 유속 판정)
- 헤더 · 탭바 · FAB · 반응형
- A4 양식 · 인쇄 CSS
- `manifest.json` · 아이콘 · 로컬 폰트

검증 실적 : 태그 균형 직전본과 동일 · 새 참조부재 0건 · CSS 중괄호 균형 0 ·
전체 변경 102행(전부 JPG 엔진 구간)
